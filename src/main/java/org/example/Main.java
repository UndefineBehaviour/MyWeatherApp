package org.example;

/**
 * Entry point. All the real logic lives in WeatherAppGUI (the Swing UI)
 * and WeatherApiClient (talks to OpenWeatherMap).
 */
public class Main {
    public static void main(String[] args) {
        WeatherAppGUI.main(args);
    }
}
